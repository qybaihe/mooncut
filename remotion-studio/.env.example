# This file is consumed only by the local import CLI, never by browser code.
# Copy to .env and keep the actual key out of Git.
BGM_SERVICE_URL=http://127.0.0.1:8787
BGM_SERVICE_API_KEY=
BGM_MAX_IMPORT_BYTES=52428800
