export const devDemoMeta = {
  slug: 'dev-demo',
  title: 'Dev demo · 11s',
  description:
    "Landscape (1920×1080) developer demo. A title lands, a terminal types the install command, a CodeBlock reveals the snippet line-by-line, and a BrowserFrame shows the result while a Cursor moves in and clicks. Cuts use glass-wipe, a zoom punch-in, and grid-pixelate — so it doubles as a transitions reel. The interface category as the lead.",
  duration: 11,
  fps: 30,
  width: 1920,
  height: 1080,
  categoriesUsed: ['interface', 'transitions', 'entrances', 'scenes'],
  category: 'developer' as const,
} as const;
