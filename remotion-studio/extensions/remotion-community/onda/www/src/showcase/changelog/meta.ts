export const changelogMeta = {
  slug: 'changelog',
  title: 'Changelog · 9s',
  description:
    "Square (1080×1080) release-notes video. A ChapterCard stamps the version, a CodeDiff reveals the actual change line-by-line, a stagger calls out the highlights, and an EndCard points to the update. The interface category narrating a real release — the kind of clip a dev tool ships every sprint.",
  duration: 9,
  fps: 30,
  width: 1080,
  height: 1080,
  categoriesUsed: ['interface', 'scenes', 'entrances'],
  category: 'developer' as const,
} as const;
