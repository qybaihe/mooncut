export const codeToPreviewMeta = {
  slug: 'code-to-preview',
  title: 'Code to preview · 8s',
  description:
    'Landscape (1280×720) hot-reload loop. A split-screen pairs a CodeBlock on the left with a BrowserFrame preview on the right: as the source reveals line-by-line, the preview reads as updating in step — the heading lands, then the CTA button — selling the edit-and-see-it feel devs live in. Interface category, side by side.',
  duration: 8,
  fps: 30,
  width: 1280,
  height: 720,
  categoriesUsed: ['interface', 'scenes', 'entrances'],
  category: 'developer' as const,
} as const;
