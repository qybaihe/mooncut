export const bentoDriftMeta = {
  slug: 'bento-drift',
  title: 'Bento drift · 9s',
  description:
    "A genuinely infinite pan across a colorful bento grid of images. Fifteen tiles of varying spans tile an 8×5 grid edge-to-edge — no overlaps, no gaps — each breathing with its own ken-burns (varied direction + origin); the Camera glides at constant velocity along a diagonal, never decelerating and never reaching an edge, while a vignette masks the bounds. The grid is larger than the frame ever shows, so the drift keeps revealing fresh tiles. A showcase for the Camera primitive.",
  duration: 9,
  fps: 30,
  width: 1280,
  height: 720,
  categoriesUsed: ['media', 'cinematic'],
  category: 'marketing' as const,
} as const;
