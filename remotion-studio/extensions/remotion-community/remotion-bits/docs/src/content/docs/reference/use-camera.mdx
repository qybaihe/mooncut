---
title: useCamera
description: Hook to get the current camera state.
---

Get just the current camera state.

```tsx
const camera = useCamera();
// { x, y, z, rotateX, rotateY, rotateZ, scale }
```
