---
title: CLI Simulation
category: Text Animations
description: Simulates a command-line interface with user typing and system output.
---
import { BitPlayground } from '@components/BitPlayground';

Simulates a realistic terminal interaction. Uses `deleteBeforeNext={false}` to append multiple lines of output without clearing the previous text, mimicking a system log or command output.

<BitPlayground client:only="react" bitName="CLISimulation" />
