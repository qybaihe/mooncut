---
title: 3D Card Stack
category: Staggered Motion
description: A stack of cards that spreads out in 3D space with staggered animation.
---
import { BitPlayground } from '@components/BitPlayground';

This bit showcases advanced usage of `StaggeredMotion` combined with 3D CSS transforms. It demonstrates how to combine staggered entrance animations (opacity, scale, y-position) with static positional offsets to create a 3D fan effect.

<BitPlayground client:only="react" bitName="CardStack" />
