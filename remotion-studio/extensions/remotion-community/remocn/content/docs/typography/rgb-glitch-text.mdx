---
title: RGB Glitch Text
description: Three RGB-offset text copies jitter for a few frames to create a chromatic aberration glitch
---

<ComponentPreview name="rgb-glitch-text" />

## Installation

<InstallBlock name="rgb-glitch-text" />

## Usage

```tsx
// src/Root.tsx
import { Composition } from "remotion";
import { RGBGlitchText } from "@/components/remocn/rgb-glitch-text";

const RGBGlitchTextScene = () => (
  <RGBGlitchText text="SYSTEM" glitchAt={20} glitchDuration={8} intensity={6} />
);

export const RemotionRoot = () => (
  <Composition
    id="RGBGlitchText"
    component={RGBGlitchTextScene}
    durationInFrames={90}
    fps={30}
    width={1280}
    height={720}
  />
);
```

## Props

<PropsTable
  rows={[
    {
      name: "text",
      type: "string",
      required: true,
      description: "The text to display and glitch",
    },
    {
      name: "fontSize",
      type: "number",
      default: "96",
      description: "Font size in pixels",
    },
    {
      name: "color",
      type: "string",
      default: "\"#171717\"",
      description: "Base text color (visible at all times)",
    },
    {
      name: "fontWeight",
      type: "number",
      default: "700",
      description: "CSS font-weight",
    },
    {
      name: "glitchAt",
      type: "number",
      default: "20",
      description: "Frame at which the glitch starts",
    },
    {
      name: "glitchDuration",
      type: "number",
      default: "8",
      description: "Duration of the glitch window in frames",
    },
    {
      name: "intensity",
      type: "number",
      default: "6",
      description: "Maximum offset (in pixels) for the RGB copies",
    },
    {
      name: "seed",
      type: "string",
      default: "\"glitch\"",
      description: "Seed used by `random()` so the jitter stays deterministic",
    },
    {
      name: "speed",
      type: "number",
      default: "1",
      description: "Playback speed multiplier (1 = normal, 2 = twice as fast)",
    },
    {
      name: "className",
      type: "string",
      description: "Optional className passed to the inline-block wrapper",
    },
  ]}
/>
