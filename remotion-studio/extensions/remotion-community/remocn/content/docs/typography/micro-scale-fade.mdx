---
title: Micro Scale Fade
description: A calm, tiny scale pop for subtle premium polish on labels and headings
---

<ComponentPreview name="micro-scale-fade" />

## Installation

<InstallBlock name="micro-scale-fade" />

## Usage

```tsx
// src/Root.tsx
import { Composition } from "remotion";
import { MicroScaleFade } from "@/components/remocn/micro-scale-fade";

const MicroScaleFadeScene = () => (
  <MicroScaleFade text="Welcome to motion." scaleFrom={0.96} />
);

export const RemotionRoot = () => (
  <Composition
    id="MicroScaleFade"
    component={MicroScaleFadeScene}
    durationInFrames={60}
    fps={30}
    width={1280}
    height={720}
  />
);
```

### With Backdrop

The component renders transparent — supply the background via `Backdrop`. It ships single-theme; edit the copied file to re-theme colors.

Pair with `Backdrop` to place the text inside a full-frame fill with a rounded, shadowed content frame:

```tsx
import { Composition } from "remotion";
import { MicroScaleFade } from "@/components/remocn/micro-scale-fade";
import { Backdrop } from "@/components/remocn/backdrop";

const MicroScaleFadeScene = () => (
  <Backdrop fill={{ type: "color", value: "#ffffff" }}>
    <MicroScaleFade text="Welcome to motion." scaleFrom={0.96} />
  </Backdrop>
);

export const RemotionRoot = () => (
  <Composition
    id="MicroScaleFade"
    component={MicroScaleFadeScene}
    durationInFrames={60}
    fps={30}
    width={1280}
    height={720}
  />
);
```

## Props

<PropsTable
  rows={[
    {
      name: "text",
      type: "string",
      required: true,
      description: "The text to animate",
    },
    {
      name: "scaleFrom",
      type: "number",
      default: "0.96",
      description: "Starting scale the text grows from as it fades in. Animates to 1.",
    },
    {
      name: "fontSize",
      type: "number",
      default: "72",
      description: "Font size in pixels",
    },
    {
      name: "fontWeight",
      type: "number",
      default: "600",
      description: "CSS font-weight",
    },
    {
      name: "color",
      type: "string",
      default: "\"#171717\"",
      description: "Text color (any valid CSS color)",
    },
    {
      name: "className",
      type: "string",
      description: "Optional className passed to the underlying span",
    },
  ]}
/>
