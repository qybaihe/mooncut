# Implementation Plans

Generated by the improve skill on 2026-07-07, against commit `361f442`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

Repo owner handles ALL git operations — executors edit files only (no branch/commit/stash/push).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Delete dead WebGL/UI components + 9 unused deps | P1 | S | — | DONE (PR #56) |
| 002 | Fix real type errors, re-enable typecheck in build | P1 | M | 001 | DONE (PR #64) |
| 003 | Port 17 vitest test files to bun:test, add test script | P1 | M | — | DONE (PR #61; 14 pre-existing fails documented in PR) |
| 004 | Patch vulnerable deps (Next ≥16.2.5, shadcn→dev, ws) | P1 | S | — | DONE (PR #57, ws override skipped) |
| 005 | Unspoofable render rate limit + queue depth cap | P1 | S | 003 | DONE (PR #63) |
| 006 | Evict failed/stale render jobs from registry Map | P1 | S | 003 | DONE (PR #65) |
| 007 | SSRF: allowlist GitHub avatar hosts in render input | P1 | M | 003 | DONE (PR #62) |
| 008 | Registry drift guard (public/r vs registry/ sources) | P2 | S | — | DONE (PR #58) |
| 009 | Clear biome lint baseline (589 → 0) | P2 | M | best after 001–003 | DONE (PR #67) |
| 010 | CI pipeline (lint, typecheck, test, registry drift) | P2 | S | 002, 003, 008, 009 | DONE (PR #68; test gate non-blocking until #61's 14 fails fixed; first CI runs green) |
| 011 | Hero video: re-encode 18 MB → <6 MB, poster, preload | P2 | S | — | DONE (PR #59) |
| 012 | In-view gating for ~13 landing Remotion Players | P2 | M | — | DONE (PR #60) |
| 013 | Lazy registry barrels (whole-catalog bundle fix) | P3 | L | 012 | DONE (PR #66) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002 requires 001: deleting `@react-three/fiber` removes the stated cause of the `@types/mdx` collision that `ignoreBuildErrors` was masking.
- 005/006/007 require 003: they modify `lib/server/*` and extend the exact test files that currently don't execute (vitest-authored under `bun test`). Land the safety net first.
- 010 requires 002+003+008+009: CI gates on `typecheck`, `test`, `registry:check`, and `lint` all being green scripts.
- 013 requires 012: both edit `bento-registry.tsx` / `ui-registry.tsx` / `interactive-code.tsx`; 013's diffs assume 012's `containerRef` shape.
- 004 is independent and can land any time; its optional ws-override step is best verified after 003 (render tests runnable).
- 009 is best run after 001–003 (fewer files, no dead code to lint-fix).

## Findings considered and rejected

- **Aurora.tsx effect re-inits WebGL on `amplitude` change** (`components/Aurora.tsx:210`): real defect, but the file has zero importers — deleted by Plan 001 instead of fixed.
- **"RENDER_SDK.md / MOTION.md / STUDIO docs are stale"**: rejected — these are active design documents for planned features (render SDK, studio, motion language), not rot. Genuinely stale: `PLAN.md` (April bootstrap plan describing a monorepo that was never the final shape), `DESIGN.md` (April), and the Architecture/Commands sections of `CLAUDE.md` (claims pnpm+turborepo monorepo with `apps/web/`; reality is a flat bun+Next.js app). A docs-refresh plan was not requested; the owner should rewrite CLAUDE.md's architecture section by hand — it misleads every agent session.
- **`public/studio.png` / `public/render-sdk.png` orphaned (745 KB each, 0 refs)**: likely assets for planned features or other branches — deletion needs owner confirmation, not a plan.
- **Sequential rendering in `scripts/render-demos.mts`**: intentional and documented; not a finding.
- **Download route deletes MP4 on client-aborted stream** (`app/api/render/[jobId]/download/route.ts:52`) and **`use-mp4-export` unmount cleanup gaps** (`app/(home)/stars/hooks/use-mp4-export.ts:40`): confirmed real, MED/LOW impact; not selected for planning this round — candidates for the next batch.
- **Two parallel docs-preview shells** (`component-preview.tsx` vs `ui-component-preview.tsx`), **tsconfig per-hook alias sprawl**, **github-stars god-file / date-fns**, **744–978 KB PNGs**, **missing editorconfig/pre-commit**: recorded, lower leverage; not planned this round.
- **Moderate/low transitive advisories** (js-yaml, postcss, qs, ip-address, brace-expansion, @babel/core): accepted noise floor after Plan 004; mostly under the dev-only `shadcn` tree.

## Direction findings (not planned — maintainer options)

1. **Extract `@remocn/render-sdk`**: render engine already duplicated on `feat/site-to-video` (two independent p-limit semaphores); studio will be a third consumer. Design doc exists (RENDER_SDK.md). Prerequisite: Plans 003/005/006/007 (tested, hardened engine first).
2. **Productize `skills/remocn` into the video builder** the business model names.
3. **Standalone camera primitive** from `a1-product-demo`'s camera-crane-up + MOTION.md's camera model.

## Audit coverage note

Standard-depth audit (4 parallel category agents + manual verification of every tabled finding). NOT audited: `content/` MDX prose accuracy, registry components exhaustively (~20 of 167 sampled), `feat/*` branches, deep Dockerfile/nixpacks review.
