# MoonCut Web · Cloudflare Pages SPA fallback
# 静态文件（/assets/*、/mediapipe/* 等）只要存在于 dist，Pages 会优先直出，
# 不会走下面的 SPA 回退。部署竞态下若短暂 404，/* 回退 HTML 可能被错误缓存，
# 因此 /assets/* 在 _headers 里强制 JS/CSS MIME，并用文件名哈希做缓存破坏。
# /api/* 由 Pages Function 处理（优先级高于 _redirects）。
/*    /index.html   200
