{
  "schemaVersion": "mooncut.lessons.v1",
  "lessons": [
    {
      "id": "impact-must-use-local-timeline",
      "status": "active",
      "learnedFrom": "agent-generated-v1.mp4",
      "trigger": "A beat uses kind=impact.",
      "rule": "The impact component must run inside a Sequence whose frame zero is local to that beat. Sample the latter half of the beat and reject the render if the expected phrase is not visibly full-screen.",
      "regression": "quality-review impact sequence must pass visual text confirmation"
    },
    {
      "id": "real-evidence-over-simulated-card",
      "status": "active",
      "learnedFrom": "User requirement: real browser and X evidence",
      "trigger": "The spoken claim references an official launch, announcement, page, product, or X post.",
      "rule": "Use browser-evidence or x-post-evidence before planning. Attach the returned evidenceId to an evidence beat. Never replace source evidence with a recreated X card or invented browser UI.",
      "regression": "captured evidence must be used and visually confirmed inside the rendered Safari scene"
    },
    {
      "id": "impact-pulse-must-match-spoken-word",
      "status": "active",
      "learnedFrom": "agent-generated-v2.mp4",
      "trigger": "A timed transcript contains a word matching an impact phrase or keyword.",
      "rule": "Persist the absolute word timestamp as impactAtMs and land the full-screen flash/ring pulse there. Never derive semantic sync from a fixed percentage of the beat duration.",
      "regression": "impact QA samples must be centered on impactAtMs and the rendered phrase must visibly land there"
    },
    {
      "id": "qa-protocol-failure-is-not-edit-failure",
      "status": "active",
      "learnedFrom": "agent regression job 89ba9779e6824b419be4730a3320b6ef",
      "trigger": "All hard QA findings are vision-gate-unavailable.",
      "rule": "Retry verification without changing the edit spec or rendering the video again. Only revise and rerender when a visual finding identifies an actual frame-level defect.",
      "regression": "a structured-output or gateway timeout must not invoke render_edit"
    },
    {
      "id": "live-worker-job-is-not-interrupted",
      "status": "active",
      "learnedFrom": "multi-instance regression job da5ae4c110bd47c094280332e246b758",
      "trigger": "A service instance starts while another local worker owns a queued or running job.",
      "rule": "Persist ownerPid on job creation and only recover a queued/running job as interrupted when its owner process is no longer alive.",
      "regression": "startup recovery must preserve jobs whose ownerPid passes a local liveness check"
    }
  ]
}
