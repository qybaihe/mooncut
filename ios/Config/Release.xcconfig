// Release uses the same Cloudflare Pages edge API as Web. It owns Cookie
// auth, billing and the authenticated tunnel to the render agent.
MOONCUT_API_BASE_URL = https:/$()/mooncut.me/api
MOONCUT_TRUSTED_HOST = mooncut.me
MOONCUT_TLS_MODE = system
MOONCUT_DISTRIBUTION_MODE = internal
